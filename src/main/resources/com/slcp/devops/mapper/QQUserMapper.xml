<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.slcp.devops.mapper.QqUserMapper">
    <insert id="AddQqUser" parameterType="com.slcp.devops.entity.QqUser">
        insert into QqUser (nickname,gender,avatar,accessToken,tokenExpireIn,openId,date)
         values (#{nickname},#{gender},#{avatar},#{accessToken},#{tokenExpireIn},#{openId},#{date});
    </insert>
</mapper>